import styled from 'styled-components';

export const LoaderStyles: any = styled.div`
  display: flex;
  justify-content: center;
  margin-left: 10px;

  svg {
    width: 24px;
    height: 24px;
    stroke: var(--white);
  }
`;
